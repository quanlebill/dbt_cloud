models:
  - name: mart__countries
    semantic_model:
      enabled: true
      name: countries_standard_name_and_code
    columns:
      - name: country_name
        entity:
          name: country_name
          type: primary

      - name: iso_alpha_2_code
        entity:
          name: iso_alpha_2_code
          type: unique
      - name: iso_alpha_3_code
        entity:
          name: iso_alpha_3_code
          type: unique